import heroImg from "/hero-image.png";

const Hero = () => {
  return (
    <header className="hero">
      <img src={heroImg} alt="Dust splash" />
      <h1>Wallpapers</h1>
    </header>
  );
};

export default Hero;
